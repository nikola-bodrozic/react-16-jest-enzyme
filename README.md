# React class component & Jest testing

`yarn` install dep.

`yarn test` shows how to test form that is submitted and an item is added to component's state

`yarn start` starts an app with form submit